# Copilot Instructions

Follow the repository agent instructions in [`../AGENTS.md`](../AGENTS.md). In particular, keep real
financial data out of the repo, preserve raw imported rows for auditability, keep SQLite behind the
Hono API, and run `pnpm verify` before handing off code changes.
